回看ResNOE算法时
之前的验证逻辑有些问题，
神经网络训练的先验模型（预测AOD和SSA），用于约束最优化方向，
因为模型在Aeronet数据上进行的微调，所以在验证时应该使用类似十折交叉验证或分出测试集验证（这种方法不太行 因为样本太少了）
所以当前结果理论上讲应该是偏好的  之前做的时候没注意到这点, 是因为当时在检验先验模型精度时的精度并不是极高（正常精度） 所以忽视了
但总的来讲 使用AOD-SSA dual约束对最优化反演的约束和框架理念是没问题的   事实证明,data_driven 确实可以提供更好的反演结果（Tao等PDL Gu等胶囊神经网络  等）
